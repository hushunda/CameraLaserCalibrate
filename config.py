config = {
        'step instruction': ['fisrt calibrate inter params','second calibrate camera to laser matrix'],
        'data_root': '',
        'remote ros': '',
        'camera_model': 'omnidir',# fish, pinhole or omnidir(Mei) model
        'tag_type': '',# 'chess' 'aruco'
        'tag_size': '',#
        'chess_borad': '', # if choice chess ,need
        }